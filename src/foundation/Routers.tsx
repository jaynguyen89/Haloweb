import React from 'react';
import { BrowserRouter, Routes } from 'react-router-dom';

const Routers = () => {
    return (
        <BrowserRouter>
            <Routes>

            </Routes>
        </BrowserRouter>
    );
};

export default Routers;
